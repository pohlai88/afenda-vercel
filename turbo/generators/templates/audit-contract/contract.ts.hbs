/**
 * Canonical {{slug}} module metadata — derive audits, tests, and guards from here.
 *
 * Generated by `pnpm gen audit-contract` ({{date}}). Action strings are
 * built via `buildCrudSapAuditAction` so new entries automatically follow
 * the `erp.<module>.<object>.<verb>` shape. Extend by adding another key —
 * never hand-author the string outside this file.
 *
 * @see lib/erp/crud-sap.shared.ts — `CRUD_SAP_VERBS`, `buildCrudSapAuditAction`
 * @see lib/features/lynx/lynx.contract.ts — reference example
 * @see lib/features/execution/execution.contract.ts — reference example
 */
import { buildCrudSapAuditAction } from "#lib/erp/crud-sap.shared"

export const {{constantCase slug}}_MODULE_ID = "{{slug}}" as const

export const {{constantCase slug}}_AUDIT_ACTIONS = {
  {{camelCase verb}}{{pascalCase object}}: buildCrudSapAuditAction({
    area: "erp",
    module: "{{slug}}",
    object: "{{object}}",
    verb: "{{verb}}",
  }),
} as const

export type {{pascalCase slug}}AuditAction =
  (typeof {{constantCase slug}}_AUDIT_ACTIONS)[keyof typeof {{constantCase slug}}_AUDIT_ACTIONS]
