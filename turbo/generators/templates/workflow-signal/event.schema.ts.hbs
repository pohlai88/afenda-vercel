import { z } from "zod"

/**
 * {{description}}
 *
 * Generated by `pnpm gen workflow-signal --module {{slug}} --event {{eventSlug}}`.
 * Refine fields before relying on this in production workflows.
 */
export const {{exportName}}EventSchema = z.object({
  organizationId: z.string().min(1),
})

export type {{exportName}}Event = z.infer<typeof {{exportName}}EventSchema>
