/**
 * Route page — `/{locale}/o/{orgSlug}/dashboard/{{slug}}`.
 *
 * Generated by `pnpm gen capability` ({{date}}). Routing-only — composition,
 * data, and audit logic live inside `#features/{{slug}}` per AGENTS.md §6.
 */
import { {{pascalCase slug}}Page } from "#features/{{slug}}"

export default function {{pascalCase slug}}Route() {
  return <{{pascalCase slug}}Page />
}
