/**
 * Zod validation contract for `{{slug}}` form data.
 *
 * Generated by `pnpm gen capability` ({{date}}). Expand as the module's
 * write surface grows; the Server Action consumes this schema and renders
 * field-level errors back into `{{pascalCase slug}}FormState` (shared module state).
 */
import { z } from "zod"

export const {{camelCase slug}}Schema = z.object({
  name: z.string().min(1).max(200),
})

export type {{pascalCase slug}}Input = z.infer<typeof {{camelCase slug}}Schema>
