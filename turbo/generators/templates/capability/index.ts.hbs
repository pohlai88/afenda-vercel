/**
 * Public import door for `#features/{{slug}}`.
 *
 * Generated by `pnpm gen capability` ({{date}}). Add new exports as the
 * module grows — never expose internals via deep imports. See AGENTS.md §6.
 */
export { {{camelCase verb}}{{pascalCase slug}} } from "#features/{{slug}}/actions/{{verb}}-{{slug}}"
export {
  countFor{{pascalCase slug}}Organization,
  listFor{{pascalCase slug}}Organization,
} from "#features/{{slug}}/data/{{slug}}.queries"
export { {{pascalCase slug}}Page } from "#features/{{slug}}/components/{{slug}}-page"
export {
  ORG_DASHBOARD_{{constantCase slug}},
  {{camelCase slug}}DashboardPath,
} from "#features/{{slug}}/constants"
export { {{constantCase slug}}_AUDIT_ACTIONS } from "#features/{{slug}}/{{slug}}.contract"
export type {
  {{pascalCase slug}}FormState,
  {{pascalCase slug}}Row,
} from "#features/{{slug}}/types"
