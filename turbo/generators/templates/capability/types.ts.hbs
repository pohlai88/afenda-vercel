/**
 * Module-local types for `lib/features/{{slug}}`.
 *
 * Generated by `pnpm gen capability` ({{date}}). Keep narrow — the public
 * surface is `index.ts`; types consumed by other modules should re-export
 * from there, never deep-imported.
 */

/**
 * Shared write-action form state for the `{{slug}}` module.
 *
 * One state shape across all Server Actions (create / update / archive / …)
 * — the verb does not change the return contract. Add new error keys here
 * (e.g. `name`, `form`) rather than introducing verb-specific state types.
 */
export type {{pascalCase slug}}FormState =
  | undefined
  | { ok: true }
  | {
      ok: false
      errors: Partial<Record<"name" | "form", string>>
    }

export type {{pascalCase slug}}Row = {
  id: string
  name: string
  createdAt: Date
}
