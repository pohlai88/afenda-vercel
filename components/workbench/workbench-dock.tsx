/**
 * L4 dock — reserved for Nexus Phase 2 (AGENTS.md §5, Nexus migration phases).
 * Returns null until the dock UI ships; WorkbenchShell keeps the slot so
 * layout/extension hooks stay stable.
 */
export function WorkbenchDock() {
  return null
}
