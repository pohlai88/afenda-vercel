/** Stable element `id` for `AppShellSubNavFloatingPanel` — must match toggle `aria-controls`. */
export function appShellSubLayoutFloatingPanelId(storageKey: string): string {
  return `floating-subnav-${storageKey}`
}
