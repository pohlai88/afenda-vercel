import { SegmentRouteSpinner } from "#components2/route-loading/segment-route-spinner"

/** Default export for org ERP surface `loading.tsx` re-exports (Nexus field). */
export default function NexusRouteLoading() {
  return <SegmentRouteSpinner />
}
