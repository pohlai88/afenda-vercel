/** ERP RBAC flags for multi-country payroll configuration and reporting. */
export type MultiCountryPayrollSurfaceCapabilities = {
  readonly canSearch: boolean
  readonly canCreate: boolean
  readonly canUpdate: boolean
  readonly canAudit: boolean
}
