/** ERP RBAC flags for offboarding initiation, approval, and checklist tasks. */
export type OffboardingSurfaceCapabilities = {
  readonly canSearch: boolean
  readonly canCreate: boolean
  readonly canUpdate: boolean
}
