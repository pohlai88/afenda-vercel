/** ERP RBAC flags for compliance statutory packs and exception workflows. */
export type ComplianceSurfaceCapabilities = {
  readonly canSearch: boolean
  readonly canUpdate: boolean
  readonly canAudit: boolean
}
