/**
 * Suspense fallback for `Dashboard.shell.skipToMain` — must stay aligned with
 * `messages/en.json` → `Dashboard.shell.skipToMain`.
 */
export const DASHBOARD_SHELL_SKIP_TO_MAIN_FALLBACK_EN =
  "Skip to dashboard content" as const
